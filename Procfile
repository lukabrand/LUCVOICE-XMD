# For Heroku deployment (single process)
worker: node index.js

# Optional local / VPS: uncomment to use PM2
# worker: pm2 start index.js --name "LUCVOICE-XMD" --attach --max-memory-restart 200M --no-daemon
